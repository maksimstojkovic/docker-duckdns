name: Docker Image CI

on: [push]

jobs:
  
  build-upload:
    name: Build Image and Upload to Docker Hub
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repo
      uses: actions/checkout@v1

    - name: Build and Upload
      env:
        DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}
        TAG: $(date +%y.%m)
      run: |
        docker build --tag silentdigit/duckdns-updater:${TAG} .
        docker login --username silentdigit --p DOCKER_TOKEN
        docker push silentdigit/duckdns-updater:${TAG}
